package com.humancloud.bms.employee_service.enums;

public enum Role {
    ADMIN,      // full system access
    MANAGER,    // manages a branch and its employees
    TELLER      // counter operations only
}
